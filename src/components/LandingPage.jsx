import { useNavigate } from "react-router-dom";
import '@/App.css';

const LadingPage = () => {
  const navigate = useNavigate();
  return (
    <div className="w-full min-h-[100vh] bg-white h-full">
      
    </div>
  )
}

export default LadingPage;